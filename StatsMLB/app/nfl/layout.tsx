import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'StrikeCast NFL · Juegos y picks', description: 'NFL en el centro de análisis StrikeCast: calendario, historial, comparador y picks.' };
export default function NflLayout({children}:{children:React.ReactNode}) { return children; }
